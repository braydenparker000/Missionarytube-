{
  "name": "missionarytube",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-first static web app deployed to Azure Storage",
  "type": "module",
  "scripts": {
    "test": "node scripts/check.mjs && node --test",
    "build": "node scripts/build.mjs",
    "pin:check": "node scripts/pin-player-libs.mjs --check"
  },
  "dependencies": {
    "gsap": "3.15.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  },
  "engines": {
    "node": ">=20"
  }
}
